[project]
name = "clawd-relay-bridge"
version = "0.1.0"
description = "Clawd Relay — local bridge process"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "websockets>=12",
    "pydantic>=2.0",
]

[project.scripts]
relay = "bridge.main:main"
