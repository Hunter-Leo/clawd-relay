{
  "name": "clawd-relay-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@clawd-relay/types": "*",
    "hono": "^4.7"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "wrangler": "^4.0",
    "@cloudflare/workers-types": "^4.0"
  }
}
