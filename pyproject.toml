[project]
name = "clawd-relay"
version = "0.1.0"
description = "Clawd Relay — Cloud event relay for AI Agent hooks"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["bridge"]
